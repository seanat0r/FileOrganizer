import type { Rule } from "../types";
interface RuleCardProps {
    rule: Rule;
    index: number;
}
export declare function RuleCard({ rule, index }: RuleCardProps): import("react").JSX.Element;
export {};
